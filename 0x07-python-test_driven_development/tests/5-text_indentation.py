#!/usr/bin/python3
"""This module prints a text with 2 new lines after each of these characters: ., ? and :"""

def text_indentation(text):
    """returns a print of text"""

    if type(text) is not str:
        raise TypeError('text must be a string')
    else:
        if 
